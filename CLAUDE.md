<!-- github-account-note -->
# GitHub account: `mikelangdon041-bot`

**Use the `mikelangdon041-bot` GitHub account for EVERYTHING in this repo** — every `git push`, every `gh` command, every commit, every credential. Remote: `https://github.com/mikelangdon041-bot/Omni`.

`omni` is the **ONLY** project on `mikelangdon041-bot`. Every other project in `my-expo-app` is `zbalmuth`, and `zbalmuth` is **never** correct here.

Never move a repo between the two accounts, never add one account as a collaborator on the other's repo, and never try to "unify" them — the split is deliberate and permanent. A 403 from `gh` means the **wrong account is active**; the fix is always `gh auth switch --user mikelangdon041-bot`, never a change to repo ownership or permissions.

---

@AGENTS.md
